# Chrome Extension

The Chrome extension was moved out of this repository and is now an independent project in:

- `../job-application-tracker-extension`

Use that folder's own `package.json` and README for build/test/install instructions.

